[project]
name = "cluster-monitor-mcp"
version = "0.1.0"
description = "MCP server for monitoring RHOAI Hive cluster resources"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.8.1",
    "kubernetes>=31.0.0",
    "uvicorn>=0.34.2",
    "httpx>=0.27.0",
]

